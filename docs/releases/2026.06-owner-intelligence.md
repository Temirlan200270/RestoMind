# Owner Intelligence OS + channels + operator perf

> Milestone 2026.06-owner-intelligence — детальный архив.

### Добавлено (2026-06-04) — Owner Intelligence OS sales-ready (P0–P6)

- **P0 Deploy smoke:** `scripts/verify_owner_intel_schema.py`; чеклист §8 в [`docs/DEPLOY_RUNBOOK.md`](docs/DEPLOY_RUNBOOK.md) (API + UI smoke).
- **P1 Revenue Copilot v3:** `upsell_pair_mining.py` (co-occurrence 7d/30d); scoring explainability в `meta_json`; Smart Sales UI (pairs, worst offers, promote today).
- **P2 Telegram production:** per-org webhook mapping (`20260604_telegram_org_mapping`); human takeover parity; channel badges + outbound routing.
- **P3 POS r_keeper:** `rkeeper_client.py`, `RKeeperPOSAdapter`, `pos_provider` в org prefs.
- **P4 Weekly digest:** полный owner digest + Monday cron dedupe.
- **P5 QA polish:** `prevented_value` formula, risk filters, shift badge actions.
- **P6 Menu/Network:** CSV cost preview; network decline reasons + weekly report UX.

### Добавлено (2026-06-04) — Telegram customer channel production (P2)

- **Миграция `20260604_telegram_org_mapping`:** `organizations.telegram_bot_username`, `organizations.telegram_webhook_secret` — маршрутизация webhook по tenant secret / bot token fingerprint.
- **`telegram_customer.py`:** `resolve_org_for_telegram_webhook`, channel helpers; inbound с resolved `organization_id`; `customer_reply` → Telegram при активном channel context.
- **Admin chats:** outbound/resend сохраняют канал (WhatsApp/Telegram); badge WA/TG/Voice в inbox + шапке диалога.
- **`phone_normalize`:** `tg:{id}` не ломается E.164-нормализацией.
- **Тесты:** `tests/test_telegram_customer_production.py` (org resolve + E2E inbound → ChatLog.channel).


- **Menu cost CSV:** preview (`POST /menu/cost-import/preview`) и apply (`POST /menu/cost-import/apply`); модалка предпросмотра в каталоге меню.
- **Owner Intelligence summary:** блок `promote_today` из copilot feed + `price_increase_candidates` с рекомендуемым `%`.
- **Network Benchmark v2:** сравнение upsell/recovery/stoplist/QA по сети, `decline_reasons` по каждому филиалу, weekly report UX.

### Добавлено (2026-05-26) — Owner Intelligence weekly digest delivery

- **`owner_digest_delivery.py`:** единый pipeline preview/send; `SystemEvent` `owner_digest.sent` / `owner_digest.failed`; Redis dedupe cron + manual cooldown `owner_digest:manual:{org_id}` (30 мин, `force` обходит).
- **Admin API:** `GET /digest/preview`, `POST /digest/send`, `GET /digest/history` в `owner_intelligence.py`.
- **UI:** карточка превью + «Отправить отчёт сейчас» + timestamp последней отправки на вкладке Owner Intelligence.
- **Тесты:** `tests/test_owner_digest_delivery.py` (preview, send, dedupe, force, Monday window, org TZ).

### Добавлено (2026-05-26) — Owner Intelligence weekly digest + QA polish

- **Weekly digest (P4):** полный Telegram-дайджест (`owner_intelligence_digest.py`) — принято, допродажи, восстановление, потери, QA high/critical, Kitchen Gate, топ-5 действий; cron понедельник 10:00 по TZ org с Redis dedupe.
- **QA Auto-Audit polish (P5):** `prevented_value = total × risk_weight × probability_by_tag`; фильтр `risk_level=high,critical` в API; shift badge с причинами риска и quick actions; `qa_risk_summary.closed_count` в summary.

### Добавлено (2026-05-26) — Revenue Copilot Quality v3

- **`upsell_pair_mining.py`:** co-occurrence mining из подтверждённых заказов (7d/30d), `get_best_pairs_for_item`, rejection frequency penalties.
- **`upsell_scoring_engine.py` v3:** mined pair scores, copilot high-margin/promote boosts, price-vs-cart penalty, offer-frequency penalty, explainability meta (`score`, `score_reasons`, `blocked_reasons`, `candidate_rank`, `alternatives_seen`).
- **`strategy_engine` / OI API / Smart Sales UI:** полный meta_json attribution, `worst_offers`, `best_pairs`, `promote_today_candidates` preview.

### Добавлено (2026-06-03) — Owner Intelligence OS (Waves 1–4)

- **Wave 1 Revenue Copilot:** `upsell_scoring_engine.py` (margin, copilot feed, anti-repeat penalties); DB anti-repeat + draft infer; `upsell_phrase_variants` + `upsell_experiments.py`; `get_copilot_candidate_lists`; Smart Sales UI + OI upsell panels; coordinator в `intent_router` / `context_engine`.
- **Wave 2 OI hardening:** cron `order_ai_audit_backfill_tick`, `owner_intelligence_digest.py`; `kitchen_gate.order_blocked` events + impact; QA audit v2 (`review_reason`, draft debounce, shift badge); Kitchen Gate expires presets (+30m/+1h/смена).
- **Wave 3 Analytics:** `cost_price` в menu API/UI + CSV import; Network Benchmark v2 DTO + full tab + weekly report.
- **Wave 4 Channels:** Telegram customer (`telegram_user_id`, `chat_logs.channel`, shared pipeline); POS adapter (`pos_provider`, `IikoPOSAdapter`).

### Добавлено (2026-05-25) — Owner Intelligence hot-path

- Upsell attribution в WhatsApp pipeline: `apply_db_upsell_rules` → `record_upsell_offer`, LLM upsell → `record_ai_upsell_from_brain_response`, infer после обновления черновика.
- QA audit после `confirm_order`; backfill `POST /api/admin/owner-intelligence/order-audits/backfill`.
- Stop-list replacements по категории + событие `kitchen_gate.replacement_suggested`; метрика в summary.
- UI: карточки аудитов с причинами, кнопки «Проверен» / «Скрыть» / «Открыть заказ».

### Добавлено (2026-05-25) — Owner Intelligence (6 этапов MVP)

- **Summary API:** `GET /api/admin/owner-intelligence/summary` — принято/спасено/апсейл/потери/AI cost/ROI, top losses & actions.
- **QA Auto-Audit:** `ai_order_audits`, детерминированный risk scoring, review/dismiss API, событие `ai_order.audit_risk_detected`.
- **Upsell attribution:** `upsell_offer_events`, impact summary API (`/upsell-impact`).
- **Kitchen Gate v2:** `operational_mode_states`, GET/PATCH `/kitchen-gate`, блокировка доставки в `decision_engine`.
- **Menu Profit Lab:** `/menu-profit`, lite-режим без себестоимости + preview в summary.
- **Network Benchmark:** `/network-benchmark` для сетей (disabled для одной точки).
- UI: под-вкладка **Owner Intelligence** в AI Center; Kitchen Gate toggles также на вкладке **Смена** (макрос `_kitchen_gate_panel.html`).
- **Миграция:** `20260525_owner_intel_foundation.py` (+ `MenuItem.cost_price`).

### Добавлено (2026-05-25) — WhatsApp duplicate phone + latency diagnostics

- **E.164 canonicalization:** `app/services/phone_normalize.py`, `user_phone_resolve.py` — единый lookup для `7705…` / `+7705…` в `get_or_create_user`, admin chats и webhooks.
- **Admin dedupe:** `adminNormalizePhone` / `adminDedupeChatListByPhone` в `admin-app.js` — один диалог в списке при legacy-дублях.
- **Queue wait metric:** `wa_queue_metrics.py` — `queue_wait_ms` в `rm_stage_ms` (enqueue → `process_with_retry` start).
- **Diagnostics:** `scripts/diag_duplicate_phones.py`, `scripts/diag_whatsapp_latency.py`, `scripts/merge_duplicate_users.py`.
- **Render blueprint:** worker `restomind-worker`, `REDIS_ENABLED=true`, `ARQ_ENABLED=true`, `DB_POOL_SIZE=3`, `APP_ENV=production` (задать `REDIS_URL` в Dashboard).

### Добавлено (2026-05-25) — Performance Pack (WhatsApp hot path)

- **Kitchen-gate:** при закрытой кухне (но открытом зале) новые заказы без `is_preorder` → `kind='night_preorder'` и утренний flow активации.
- **Quick replies:** детерминированные ответы без LLM (`greeting`, `thanks`, `operator`, `cancel`, `working_hours`) — `app/services/quick_replies.py`, toggle `QUICK_REPLIES_ENABLED`.
- **FAQ cache:** Redis-кеш ответов `intent=faq` по `(org_id, hash вопроса, kb_fingerprint)` — `FAQ_CACHE_ENABLED` (default on).
- **Prompt metrics:** замер размера промпта + обрезка `chat_history` при превышении `PROMPT_MAX_TOKENS_SOFT`.

### Добавлено (2026-05-25) — Performance Pack phase 2

- **Event pipeline async:** analytics / audit / healing после commit родительской транзакции (`event_consumer_runner.py`, `EVENT_CONSUMERS_ASYNC=true` по умолчанию; тесты — sync через `EVENT_CONSUMERS_ASYNC=false` в conftest).
- **Parallel LLM context:** `build_menu_context_for_ai` и `build_sales_strategy` параллельно в `build_llm_prompt_bundle`.
- **Admin perf:** lazy-mount `dashboard` / `menu` / `ai_center` / `marketing`; lazy chunk `admin-marketing.js`; long-cache на versioned `/static/*`.

### Добавлено (2026-05-25) — Performance Pack phase 3

- **FAQ cache metrics:** `GET /api/admin/system/faq-cache-metrics` — hit/miss/save и `hit_rate_pct` по `rm:metrics:faq_cache:*`.
- **Quick replies:** детерминированные «меню» и «статус заказа» без LLM.
- **Context fetch:** `fetch_ai_read_context` — одна DB-сессия вместо шести параллельных (меньше pool checkout / roundtrip).

